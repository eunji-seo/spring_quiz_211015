<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
 <mapper namespace="com.quiz.lesson05.dao.WeatherHistoryDAO">
 	<insert id="insertWearherHistory" parameterType="com.quiz.lesson05.model.Weatherhistory"
 	useGeneratedKeys="true" keyProperty="id">
 		insert into `Weatherhistory`
 		(
 			`date`
			,`weather`
			,`temperatures`
			,`precipitation`
			,`microDust`
			,`windSpeed`
			,`createdAt`
			,`updatedAt`
 		)
 		values
 		(
 			#{date}
 			, #{weather}
 			, #{temperatures}
 			, #{precipitation}
 			, #{microDust}
 			, #{windSpeed}
 			, NOW()
 			, NOW()
 		)
 	</insert>
 	<select id="selectWearherHistory" resultType="com.quiz.lesson05.model.Weatherhistory">
 		select 	
 			`id`
 			,`date`
			,`weather`
			,`temperatures`
			,`precipitation`
			,`microDust`
			,`windSpeed`
			,`createdAt`
			,`updatedAt`
 		
 		from
 			`Weatherhistory`
 		where
 			`id` = #{id}
 	</select>
 	
 </mapper>
 